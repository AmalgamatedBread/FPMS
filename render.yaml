services:
  - type: web
    name: fpms-backend
    env: java
    buildCommand: mvn clean package -Pprod -DskipTests
    startCommand: java -jar target/fpms-backend-*.jar
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: JAVA_VERSION
        value: 21
    disk:
      name: uploads
      mountPath: /opt/render/project/src/uploads
      sizeGB: 1